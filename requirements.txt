# bring-back-BOLO - Dependencies (runtime + developer)
# Install with: `pip install -r requirements.txt`

# --- Runtime dependencies ---
# Core game engine / multimedia
pygame>=2.1

# Optional (recommended) libraries for math and utilities
# numpy: Helpful for vector math, physics, and fast array ops (optional)
numpy>=1.23

# Networking (optional) - used when building multiplayer prototypes
websockets>=10.0
aiohttp>=3.8

# --- Development / testing tools ---
# pytest: unit and integration tests
pytest>=7.0
# black: code formatter
black>=22.3.0
# mypy: static type checking
mypy>=1.0

# Notes:
# - Keep runtime dependencies minimal for players; put heavy tooling only
#   in development workflows or optional extras (e.g. `requirements-dev.txt`).
# - If you prefer separate dev deps, I can split these into `requirements-dev.txt`.
